# STATE — где мы

**Фаза: инструментом пользуется живой логопед, и его правки чинят материал.**
Движок закрыт · шесть листов · **сайт на своём домене — https://logozvuk.com**, шесть страниц,
счётчик и канал обратной связи работают · PDF собирается на одной странице у всех материалов.

**🟢 РУБЕЖ СДВИНУЛСЯ 08-26 (вечер): живой логопед пользуется инструментом и прислал правку
по существу — вторую за день.** «Добавьте, пожалуйста, гласных и, у нас же 6 гл звуков» — со
скриншотом страницы, снятым **с телефона**. То есть материал больше не «не видел живого логопеда»:
его смотрят, по нему задают вопросы, и вопросы попадают в настоящие дыры (ложь кнопки, мобильный
экран). Дословно и с разбором — `ГОЛОСА.md`.
⚠ Строка «ни один материал не видел живого логопеда В РАБОТЕ» держалась в этом файле дольше правды
и один раз была пересказана как текущее положение. Сверять её теперь по `ГОЛОСА.md`, а не по
памяти. Чего по-прежнему НЕТ: свидетельств, что лист печатали и вели по нему занятие.

**Но голоса уже пошли, и второй из них перестроил экран.** Логопед в группе 08-26: «нельзя ли на
каждое занятие по несколько листов, а то только один вариант на каждый звук?» — и на уточнение
ответила лестницей: «на автоматизацию звуков в слоге, слове, фразе, тексте». Это канон. Ряд
вкладок теперь назван им (`ГОЛОСА.md`, `DECISIONS.md` 08-26).

**Рождение:** 2026-08-02, `/seed`. Отпочковался от `app-studio/research/`.
**Гит:** `github.com/PanarinI/logoped-generator`, приватный. Ветка эксперимента —
`syuzhetnye-listy` (в прод не идёт).
**Трек по материалам — `INSTRUMENTY.md`. Разрез листа глазами —
`schemas/2026-08-08-anatomiya-lista-v2.svg`.**

## 🆕 08-27/28 — история интерфейса восстановлена и показана

Автор попросил увидеть, как менялся экран от первой версии к нынешней. **Двадцать состояний подняты
из git и сняты одинаково**: `git archive <sha> web logoped_slovar` → сервер этой версии на 10 секунд
→ headless-Chrome, окно 1440×1050. Клики (выбрать [Р], дойти до листа) имитированы вшитым в
`index.html` драйвером — вид интерфейса он не трогает. Две вещи снялись отдельно: мокап 08-02
(до кода) и первая проба листа 08-01.

**Что видно в истории** (сложено в две публикации, ссылки у автора): 08-02 мокап уже показывал лист
сразу → 08-05 первый код ушёл в МАСТЕР из трёх шагов → 08-18 шаг «тип слога» снят (влиял на 3
материала из 7) → 08-19 экран «Что делаем?» со ступенями → 08-21 мастер снят целиком (обмер 29
чужих генераторов: мастеров два, оба ради оплаты) → 08-22 экран перестроен вокруг листа →
08-26 вкладки названы ступенями по словам логопеда, ряд уехал наверх. Кубик прошёл путь «↻ Другой
вариант» → «Другие слова» → «Собрать другой набор слов» → знак кубика → охряный слева во всю высоту.

⚠ **Исходники снимков лежат во временной папке сессии** (`scratchpad`), а не в проекте: они
переживают сессию только в опубликованных лентах. Если история интерфейса нужна как актив — сказать,
и я перенесу 20 кадров в `kadry-interfeysa/` (≈8 МБ) вместе со скриптом съёмки.

## 🔴 СЛЕДУЮЩИЙ ШАГ

**Показать инструмент логопедам — объявление написано и ждёт отправки.** Всё готово в
`research/site/anons/`: пост (одобрен 08-25), письма на 14 сообществ (`pisma_2026-08-25.md`),
три картинки, разведка ВК (73 сообщества по бакетам) и ТГ.
**Первая — Дарья Романова `vk.com/logoseti`, 31,5 тыс.:** год назад сама сделала видеообзор
инструмента автора. Бюджет 5–15 тыс. ₽, тратить волнами: Дарья + одна стена → пауза 3–5 дней →
остальное, если сдвинулись счётчики (Метрика · отзывы · рост группы со 149).

⚠ **Сначала — ответить логопеду, она написала дважды.** Ответ на второе письмо (про гласную И)
СОБРАН и лежит готовым: текст плюс два снимка настоящих листов — [Р] с рядом «ра ро ру ры» и
[Рь] с «ря рё рю ри» и колонкой «Ри». Суть ответа: гласных шесть, но с одним согласным сочетаются
пять — [и] после твёрдого не встаёт, «ри» это уже мягкий [рь], отдельная закладка. И честно
сказать, что она поймала настоящую ошибку: кнопка показывала гласную, лист печатал слог, и на
мягких они расходились. Её слова — единственный живой сигнал, который у нас есть.

**Если тянет не наружу, а внутрь** — три готовых узла, любой берётся с места:
1. ~~**Пустышка в ряду гласных**~~ — **закрыто 08-26 (вечер).** В ряду звуковой дорожки стоит
   единица без гласной: «КР · КРА · КРО · КРУ · КРЫ · КРЭ». Первая, но не умолчание.
2. ~~**Спираль на звуковой дорожке**~~ — **закрыто 08-26.** Линию чертит код, герой и цель встают
   из банка, в центре стоит награда или САМ СЛОГ. Разбор — в `INSTRUMENTY.md`.
3. **Ветка `syuzhetnye-listy`** — полотно неба целым листом (второй шаг плана), потом остальные
   тринадцать миров.
4. **Связка межгласного ряда — потолок 3/4.** Банк дорощен 08-26, но два ряда из четырёх
   («ожо», «ужу») своего слова не получат никогда: таких сочетаний нет в русском. Стабильные
   4/4 упираются в ОТБОР блока [4] — он не знает про связку. Развилка автора: (а) отбор
   резервирует до 4 слов под связку — тогда слоговой блок начинает диктовать, какие слова
   увидит логопед; (б) связка на межгласном обещает не «тот же слог», а «та же гласная ПЕРЕД
   звуком» (ожо — ёжик, ужу — лужа) — тогда своё слово есть у каждого ряда, но точного
   совпадения больше нет. Замеры — в `INSTRUMENTY.md`.
5. **ИИ-кнопка на листе — идея автора 08-26, НЕ начата.** «Логопед вписывает любое изменение
   для листа, который видит на экране, и генератор выдаёт то, что запросил». Разбор: ценность
   настоящая — это последняя миля между генератором и ЛИЧНЫМ листом. Но лист сейчас выверен
   (чистота по профилю, тип Марковой, правила 7 · 8 · 11), и модель, свободно переписывающая
   лист, молча ломает каждую гарантию — на бумагу к ребёнку это уедет без предупреждения.
   Честная конструкция другая: **ИИ не переписывает лист, а переводит слова логопеда в РУЧКИ
   генератора**. «Слова покороче, убери чистоговорку» → `markova_max=4`, снять блок [7] →
   движок пересобирает лист сам, со всеми воротами и линтером. Чего ручки не умеют — модель
   говорит прямо, а не выдумывает. Сверх удобства это ещё и ДАТЧИК СПРОСА: чего логопеды просят
   и чего ручки не умеют — готовая карта следующих ручек, первоисточник вместо наших догадок.
   Чего делать НЕ стоит: «картинка на входе → картинка на выходе» — дорого, недетерминированно
   и ломает закон дома (рисуем цветное, ч/б снимаем технически); лист это текст и геометрия, а
   не картинка. Очередь: окупается только когда листами кто-то пользуется, а главный рубеж
   прежний — объявление логопедам ждёт отправки. Форма — отдельная ветка, как и сказал автор.
6. **ВЕДРО ДЛЯ СЛОВ — вопрос автора 08-26, разобран, не начат.** «Логопед закидывает слово, а на
   сессии разбора движок разбирает его по всем правилам и добавляет в картотеку или выкидывает».
   Возможно, и бо́льшая часть уже есть. Прогон настоящих слов показал, что движок сам решает:
   есть ли звук В ЗВУЧАНИИ («лось» отсеян — ровно та ошибка, за которую логопед ругала
   LogoKonspekt), позиция и типы слога, тип Марковой, прочие корригируемые (то есть фильтр по
   профилю ребёнка), оглушение на конце, сверка скелета, дубли. Человеку остаются ровно ворота
   брифа §6: изобразимо ли одним предметом · не чувствительное ли · знакомо ли ребёнку 4–7
   (familiarity, age_min) · род и мн. ч. для игр · ambiguous. И УДАРЕНИЕ: движок его не угадывает,
   слово принимается с ним.
   Форма: приёмник рядом с «На листе что-то не так?» (поле «не хватает слова»), запись append-only
   в `/data/`, разборщик `tools/vedro_slov.py` раскладывает на три кучи (✅ прошло машину ·
   🔶 нужен человек · ❌ отказ С ПРИЧИНОЙ СЛОВАМИ). Отказ здесь ценен не меньше приёма: он
   отвечает логопеду по существу и учит.
   ⚠ **Что чинить ДО этого:** генератор банка [Ж] не воспроизводит свой файл (узел 6) — путь
   «добавить слово сборкой» на нём сейчас сломан. И нужен тир происхождения: чьё слово, когда
   пришло, кто проверил, — иначе через месяц никто не скажет, откуда взялось слово.
   Связь: это дешёвый и безопасный частный случай ИИ-кнопки (узел 5) — логопед просит ДОБАВИТЬ
   СЛОВО, а не переписать лист.
7. **Три хвоста от вечера 08-26, все три — решение автора.**
   · **Колонка «Же» переносится строкой.** Колонки Э теперь ставятся ПО ДАННЫМ, и у [Ш] пятая
     колонка встала в один ряд, а у [Ж] уехала вниз одна: её слова длинные («медвежонок»), и
     пятая колонка не влезает ровно на **1 мм** (нужно 33.8, есть 32.8 при кегле 18 пт —
     канонный минимум). Варианты: терпеть перенос · дать допуск в 1–1.5 мм с оговоркой, что
     ширина считается оценкой «полсимвола на кегль», а не замером · не ставить Э у [Ж].
   · **Пятая гласная в СЛОГОВОМ РЯДУ** (не в колонках). Развилка стоит с 08-03 и не закрыта:
     с Э линтер ловит 30 нарушений вместо 24, потому что ряд растёт на четверть и ломает
     правило 11 («слоговой блок ≤ ¼ словесного»). На звуковой дорожке Э есть — там слов не надо.
   · **ИР на обратном слоге.** Вопрос логопеда про И вскрыл настоящую дыру не там, где она
     искала: «мир» = [м'ир], «пир», «тир», «кефир» — [и] перед ТВЁРДЫМ [р], то есть ИР законный
     обратный слог. У нас его нет ни в ряду, ни колонкой; то же с ИЛ, ИС, ИШ. Материал в банке
     частично есть (ир: катер·ветер·свитер · ис: 15 слов), самих «мир·пир·тир» в картотеке нет.
8. **Генератор банка [Ж] не воспроизводит свой файл.** `gen_words_zh.py` падает на своей же
   встроенной сверке: 45 расхождений в `other_correctable` — движок с тех пор стал считать
   корригируемыми и Б·П·Т·Д, а списки в генераторе писаны руками до этого. Проверено на
   НЕТРОНУТОЙ копии из гита — беда не наша, она пришла раньше. Поэтому слова 08-26 положены в
   `words_zh.jsonl` хирургически, а не сборкой. Пока это так, банк [Ж] правится только руками.

## Линия «сайт-сервис» — что стоит на 08-25

*Ниша (замеры 08-19…08-21, не меняются).* Анкор — **«автоматизация звука»** (27 210 точной/мес;
ядро `автоматизация звука р` — 4 099). Лучшего анкора нет: «логопедический тренажёр» — товарный
запрос, «карточки на звук» кластеров не дают, «речевой материал» — 5 запросов. Кластеры канона —
другие звуки: Л 3 473 · Ш 2 338 · С 1 717 = 7 528 при пороге 2 000. Чистоговорки (9 033) и
дифференциация (6 027) — около-тематики, канон запрещает брать их сразу.

*Гейт.* Выдачи расходятся: в Яндексе 0 нишевых по анкору, в Google — 5. **«Ниша свободна» снято,
конкуренты есть**, но ссылочный барьер низкий (150–200 доменов против 1 412 у эталона), Authority
Score выше 40 только у порталов. Софта в выдаче нет ни у кого: статьёй не публикуются 45 056
вариантов листа.

*Техника — всё стоит.* Домены куплены у Timeweb 08-22: `logozvuk.com` (сайт) и `logozvuk.ru`
(защитный, 301 на .com; на 08-25 ещё не делегирован). NS у Timeweb — записи правятся там.
Хостинг Amvera, 290 ₽/мес, регион Москва, деплой вебхуком из гита. `SITE_URL` применился:
`robots.txt` и `sitemap.xml` отдают боевые адреса. **`www.logozvuk.com` заведён 08-25** — домен
добавлен в Amvera, сертификат выпущен, запись в DNS уже была; наш сервер отдаёт 301 на основной
адрес тем же кодом `_canonical_host`, что и для `.ru`.

*Страницы.* Из шестнадцати опубликованы **шесть**: `/` · `/avtomatizaciya-zvuka-r/` ·
`/profil-rebenka/` · `/chem-otlichaetsya-ot-kartoteki/` · `/o-proekte/` · `/kontakty/`. Текст
нарезан из `TEKST.md` — ни строки новой прозы. Остальные десять (доки, «для кого» с подстраницами)
остаются черновиками: канон разрешает стартовать с главной и двух-трёх категорий. Снимки на
страницах даны ВЫРЕЗКАМИ с обводкой (режет CSS поверх целого PNG — пересъёмка листов их не ломает).
Так закрыт ШАГ 5 канона; из ШАГа 6 не хватает Вебмастера и Search Console.

*Текст о сервисе.* Живёт в `TEKST.md` (корень капсулы) — единственный дом; research-файл
`shag2_material_i_chernoviki_2026-08-24.md` стал сырьём. Объём 4 292 знака при вилке 2 000–4 500.
Пункт 4 «в каких ситуациях помогает» написан Клодом по просьбе автора из реальных обращений —
отступление от канона названо вслух в DECISIONS. Макет страницы — `research/site/preview_tekst.html`.

## Где мы — снимок на 2026-08-26 (вечер)

### Экран
Лента этапов сверху во всю ширину: **Все этапы · Звук · Слог · Слово · Фраза · Текст**, заголовок
«ЭТАП АВТОМАТИЗАЦИИ». Звук вертикально слева, лист в центре, настройки справа. Угол двух осей —
диагональ с подписями «этап» и «звук» (на узком экране его нет: там осей две не бывает).
Выбранное помечено акцентом — цвет в букве плюс полоса 2 px, у этапа снизу, у звука со стороны
листа; подложка бледно-зелёная, потому что белая сливалась с бумагой.
Жанр листа называет плашка под лентой; где жанр один — одна залипшая кнопка.
Кубик охряный, слева от «Скачать», подпись у него своя на каждом материале.
**«Как это устроено» — единственная дверь под листом**, и ров живёт ВНУТРИ неё первым блоком:
сперва числа этого звука («Картотека [Ж] 88 → убрали 17 → подходит 71 → на лист попало 16»,
с единицей «слов»), потом «Чего у звука не бывает» с причиной от движка, потом устройство жанра.
Отдельной ссылки «Из чего собран этот лист» больше нет — за день она дважды оказывалась второй
такой же кнопкой рядом с первой.
Ряд слогов вместо ряда гласных: на кнопке стоит СЛОГ, который встанет на лист («РЯ», «ЖИ», «КР»).
**Телефон:** ряд звуков и ряд ступеней переносятся, всё видно без свайпа; «Скачать» и кубик стоят
НАД листом; место под подписью резервируется (считаются все ВИДИМЫЕ подписи, а не первая).
Экран сам обновляется до текущей версии при любом действии (`/api/version`), кроме случая, когда
лист правлен руками, — там спрашивает.

### Материалы
| ступень | лист | состояние |
|---|---|---|
| Все этапы | лист занятия | 8 блоков, вся лестница за приём |
| Звук | звуковая дорожка (1–3 дорожки, спираль на 1) | ✅ |
| Слог | **слоговая дорожка** (первая) · звуковая дорожка со слогом (2–3) | ✅ |
| Слово | лабиринт | ✅ |
| Фраза | словосочетания | ✅ |
| Текст | пересказ · сочини сам | два РАЗНЫХ листа, выбор в плашке |

### Картинки — все банки цветные, ч/б снят с цвета
- `pictures/fony/colour` + `bw` — 35 спрайтов сцены. Цвет каждого предмета назван таблицей в
  `scenes_prompts.json`; без неё 63% банка ушло в жёлто-оранжевый.
- `pictures/dorozhka/colour` + `bw` — 21 вид героя (**одушевлены 08-26**: глаз и улыбка) ·
  35 целей · 14 спиралей. Прежние «бездушные» — в `bezdushnye_2026-08-26/`, в гите.
- `pictures/metki/colour` + `bw` — три старта и три финиша. На слоговой дорожке метка стоит НАД
  кружком, а сами концы маршрута названы толстой обводкой кружка и заливкой на цветном листе.
- Прозрачность сцены: ч/б 0.42, цвет 0.78 — иначе краску не видно. У фона листа-спирали свой
  уровень (0.62 / 0.38): он полноценный рисунок в вес героя, а не вектор в один штрих.
- **У всех картинок банка внешний белый фон СНЯТ** (251 файл, `tools/prozrachny_fon.py`): спрайт
  больше не закрывает дорожку белой коробкой. Заливка идёт от краёв, внутренняя белизна цела.
- **У линии дорожки есть ореол**: путь рисуется дважды, снизу белый и толще. На бумаге не виден,
  поверх фона отрезает дорожку от мира.
- Картинки несут **штамп содержимого в адресе** (`?v=…`) и потому кэшируются на год: сменился
  рисунок — сменился адрес. Голый адрес по-прежнему ревалидируется.

### Рассказы
**44 текста, по четыре на каждый звук** (было 31, дыра на Зь · Ль · С · Щ). Каждый выверен
`check_text` и перепроверен при вписывании — ноль нарушений. Кубик на пересказе спрятан: лист
собран из готового текста, вопросы написаны руками, движку там нечего крутить.

### Стечения — ворота на ОБОИХ концах слога
**Начало:** `onset_frames.json` — 27 законных начал со словом-доказательством. Небывалые слоги
(«рша», «рса», «нза», «рща») не печатаются. У [З] и [Щ] законных начал нет — `no_onset`.
**Конец (08-26, вечер):** ворот не было вовсе, и дорожка [Ж] печатала «АЖНЬ · ОЖДЬ · ЫЖДЬ».
Причина языковая: стечение внутри слова почти всегда открывает СЛЕДУЮЩИЙ слог («лы-жник»,
«ко-шка»). Замер: настоящих закрывающих 31, мнимых 197. Теперь рамка законна, только если реально
стоит в `coda` слога с целью — доказательство СЧИТАЕТСЯ слогоделителем, а не пишется таблицей.
Осталось: [р] арк·арт·арф·арщ · [л] алк·алп · [ль] альт·альм · [с] аст·аск · [з] азл.
У [ж], [ш], [щ], [рь], [сь] тип не предлагается — `no_coda`.

## Экран перестроен по лестнице автоматизации (08-26)

Повод — второй голос из группы: на вопрос «какие листы нужны на занятие» логопед ответила
лестницей («на автоматизацию звуков в слоге, слове, фразе, тексте»). Это канон, и он же объясняет
её первую фразу «только один вариант на каждый звук»: она видела «Занятие», где все ступени
смешаны в блоки, а ступени у нас есть отдельными материалами — просто были названы нашим языком.

**Ряд вкладок = ступени:** `Занятие` (отдельно, над рядом) · **Где закрепляем звук?** →
`Звук` · `Слог` · `Слово` · `Фраза` · `Текст`. Жанр листа называет плашка «Какой лист?» — она стоит
ВСЕГДА, даже когда жанр один (место второму размечено заранее). Этап помнит последний жанр.

**«Занятие» — вся лестница на одном листе:** разминка · звук · слоги · слова · вставь слог · игра ·
предложения · чистоговорка, то есть все ступени по одному-два задания за приём. Листы ряда —
наоборот, ОДНА ступень вглубь. Поэтому «Занятие» вынесено из ряда: оно не точка на лестнице.

**Галочка «Со слогом» снята — ступень стала вкладкой.** Звуковая дорожка стоит под `Звук`
(изолированный) и под `Слог` (со слогом) и называется одинаково. Это не дублирование: имя
закреплено за ПРИЁМОМ, а не за ступенью — ✅ формула картотек (nsportal, 15.01.2015) накрывает обе
прямым текстом («тянет нужный звук, ЛИБО соединяет закрепляемый звук с каким-либо гласным»),
✅ Попова 2022 показывает без гласной, ✅ Борисова 2008 — со слогом, ✅ Ольга прислала десять
образцов «линия + слог» под этим же именем.
На ступени `Слог` единица дорожек ЕСТЬ (08-27): в центре спирали печатается слог, ровно как
у финиша полосы. Строка «единицы нет, спираль — цельная картинка» была верна ровно сутки
08-26 и пережила материю на день — закон 17.

`S.tab` по-прежнему держит МАТЕРИАЛ, ступень — имя на экране; адреса `?material=…` целы.

---

## Сделано 08-26 — день целиком (коротко; построчно — `INSTRUMENTY.md`)

**Утро и день.** Текстотека доведена до 44 рассказов (по четыре на звук). Фоны сцены стали
цветными, цвет каждого предмета назван таблицей в `scenes_prompts.json`. Герои дорожки одушевлены
(глаз и улыбка), таблица зеркал пересмотрена. Экран перестроен по лестнице автоматизации.

**Вечер — двенадцать правок, и все, кроме одной, пришли ГЛАЗАМИ автора или логопеда.**
Штамп версии в адресе картинки · связка «слог — слово» перестала повторять одно слово ·
щётка починена дважды (полупрозрачный контур и «зубная чистит обувь») · спираль чертится кодом,
в центре награда или слог · мир вокруг спирали нарисован фоном (герой — референсом стиля) ·
кнопка показывает слог, а не гласную · пустышка «ДР» · телефон (ряды переносятся, «Скачать» над
листом) · ров переехал внутрь «Как это устроено» · зеркало гоночной машины · стечения в конце
слога перестали выдумываться · ореол у линии · белый фон снят у 251 картинки · кубик лабиринта
берёт разный набор · колонки Э по данным.

⚠ **Ни одну из этих бед не поймал ни один тест** — 5270 проверок зелёные и линтер чист во всех
случаях. Файлы на месте, имена те же, а на бумаге у ребёнка «АЖНЬ». Это цена закона 6, и она
подтверждена двенадцать раз за вечер.
---

## Защита инструмента — одна страница целиком (08-27)

`research/zashchita_instrumenta_2026-08-27.html` — устройство, словарь, метод проверки и границы
доказанного на одном листе. Опубликована артефактом:
https://claude.ai/code/artifact/4cb5a4b6-6851-4e3a-8c90-e08127d1b891
Просьба автора: «условно защитить диссертацию по инструменту».

Девять разделов: предмет · задача (нормативные числа) · РОВ · словарь · движок (один вход —
много выходов) · метод доказательства (тиры · правило-против-таблицы · две сборки · отмывка тира) ·
18 законов · источники по тирам · пределы. Числа сняты кодом с рабочего дерева, не по памяти:
11 звуков · 1596 существительных · 356 глаголов · 253 прилагательных · 980+980 рисунков ·
44 рассказа · 5278 проверок в шести тестовых файлах.

⚡ **Фильтр чистоты на странице не описан, а работает**: 1596 слов и логика движка перенесены в
браузер; сверка JS-логики с `content.is_clean` на 330 случайных профилях дала ноль расхождений.
Логопед может выбрать звук, отметить непоставленные звуки и увидеть, какие слова уходят с листа.

Палитра и типографика взяты у самого инструмента, а не придуманы: токены `web/app.css`
(бумага · чернила · зелёный акцент · охра · красный = ✅ 🔶 ⛔) и PT Sans — тот шрифт, которым
набран каждый печатный лист.

---

## Открытые узлы

0б. ⚠ **ОСТАТОК ПО СТЕЧЕНИЯМ: СВЯЗКА «СЛОГ — СЛОВО» НА ЛИСТЕ.** Слог теперь всегда настоящий
   русский, но СЛОВО рядом с ним может его не содержать: «вши — кувшин» (кув-шин), «кса — такса»
   (так-са), «джа — пиджак» (пид-жак). Стечение законно как начало слога вообще, но в ЭТОМ слове
   стоит на стыке. На звуковой дорожке безвредно — слова там нет; на листе связка печатается.
   Чинить нужны границы слогов по каждому слову (в разборе есть `syllable_idx` — возможно, хватит).

0. ✅ **СТЕЧЕНИЯ ПОЧИНЕНЫ 08-26.** Была таблица `logoped_slovar/onset_frames.json` — 27 законных
   начал слога, у каждого слово-доказательство; собрана двумя независимыми разборами со сверкой
   (метод падежей 08-10), слово проверено программой посимвольно, расхождений ноль, 15 кандидатов
   выброшены единогласно. Отсев работает и на дорожке, и на листе. Заодно снято оглушение в печати
   («ФС» → «вс», «ФША» → «вша») и починен выбор стечения на мягких звуках (экран слал «прь»,
   движок сравнивал с «пр\''» — выбор молча не срабатывал). У [З] и [Щ] законных стечений нет
   вовсе: кнопка «кРа» на них снята, причина — код `no_onset`, предел ЯЗЫКА, а не нехватка слов.

   Прежняя запись о болезни:

0а. 🔴 **СТЕЧЕНИЯ НА [Ш] И [С] — НЕБЫВАЛЫЙ СЛОГ. Найдено замером 08-26** (вопрос автора: «почему
   именно кр гр бр тр? это методика?»). Ответ: список НЕ методический. Рамки достаёт из картотеки
   `content._cluster_frames`: берёт слова, прошедшие профиль → вырезает согласное стечение из
   живого слова → режет от начала стечения до целевого звука → 2–3 согласных → выбрасывает
   сливающиеся пары → **сортирует по частоте в НАШЕЙ картотеке**, первые четыре.
   Из метода здесь только два: ✅ что стечения отрабатывают (Спивак) и ✅ что согласный стоит
   в начале пути (закон 8). Сам список — 🔶 частота, не канон.

   На [Р] совпало: `кр` (краб, крот, укроп) · `тр` (тетрадь, метро, труба) · `гр` (сугроб, груша,
   игрушка) · `бр` (брат) — настоящие начала слогов. **На [Ш] и [С] не совпало:**

   | звук | печатаем | откуда вырезано |
   |---|---|---|
   | Ш | `рша` · `фша` · `пша` | гор-шок · кув-шин · лап-ша |
   | С | `кса` · `рса` | так-са · бар-сук |

   Стечение вырезано **через границу слога** — «рша» в начале слога в русском не бывает.
   Это закон 7: правило стоит на признаке, которого в данных НЕТ — является ли стечение настоящим
   началом слога. Модуль от этого защищался, но только от четырёх согласных («ыртк»), не от двух.
   ⚠ Чинить правилом нельзя — нужен разбор, как делали падежи 08-10.

0в. 🔴 **СЛОВОСОЧЕТАНИЯ СТОЯТ НА ТЕМЕ, А ПРИЛАГАТЕЛЬНОЕ СПРАШИВАЕТ ПРО СВОЙСТВО. Замер 08-27**
   (автор увидел «вязаную заколку» и «закрытую бензопилу»). ✅ Из 8227 пар, какие движок вообще
   может напечатать, **3200 (39 %) стоят на признаке, которого в данных нет**; на бумаге это
   4,2 пары на листе из 12 — про них мы не знаем, верны они или нет («кожаный пиджак» стоит там же).
   🔶 Явной бессмыслицей из выборки в 246 выглядит около трети — 1–2 пары на лист.
   Диагноз: `combinability.json` связывает класс ТЕМЫ («предмет», «одежда») с классом
   прилагательного, а заколка лежит в теме «одежда/аксессуары» и оттуда получает «вязаную»,
   бензопила лежит в теме «предмет» и оттуда получает «закрытую». Дробление тем это не чинит —
   его уже делали 08-08, и брак вернулся другими словами (закон 4: неверен не класс, а система
   координат). Критерий отсева словами автора: **признак применим, если возможны ОБА полюса** —
   «открытой бензопилы» не бывает ⛔, «сухая роза» бывает, значит «мокрая роза» законна ✅.
   Лечение: мерки на СУЩЕСТВИТЕЛЬНОМ (из чего бывает · умеет · чем меряется), узкое
   прилагательное спрашивает мерку, широкое (цвет · размер · чистота) работает как сейчас.
   Цена: размечать надо не весь словарь, а 854 существительных — те, до кого узкие
   прилагательные дотягиваются, — по 3 вопроса на слово, всего 2572.

   ✅ **[Р] РАЗМЕЧЕН 08-27** (94 слова · 398 вопросов · 148 мерок; два независимых прохода,
   по слову и по мерке, сошлись без расхождений). Из 1763 пар [Р] мерка убрала 295 (17 %),
   осталось 1468 — на лист идёт 12, потери нулевые. Ушли «круглый арбуз» · «твёрдый трактор» ·
   «прозрачное метро» · «мокрый костёр» · «кирпичный город» · «жаркая нора».
   Механика: `merka_prilagatelnogo` в `combinability.json` (61 слово) + поле `merki` в карточке
   существительного. ⚠ `slova_razmecheny: ["р"]` — КОСТЫЛЬ СТРОЙКИ: у неразмеченного звука
   проверка молчит и блок работает по старой таблице тем. Обязан исчезнуть, когда список
   дорастёт до одиннадцати. **Осталось: 760 слов десяти звуков, ~2170 вопросов.**

   ✅ Проверены обе встречные мысли автора. «Откатить дробление 08-08» — откатывать нечего:
   без узких прилагательных 14 классов сливаются только в 12, остальное нужно широким; откат
   вернул бы «кирпичную руку». «Просто пройтись по парам» — способ рабочий, глазами работа та
   же, разница только в том, где записан ответ: сейчас 3200 решений против 2572, но новое
   ПРИЛАГАТЕЛЬНОЕ стоит 43 решения при списке пар и 1 при мерках. Фильтр от пополнения словаря
   не сбивается, а замолкает: неразмеченное слово просто не даёт узких пар.

   Всё с числами и перечнем — `research/logoped_sochetaemost_zamer_2026-08-27.md`,
   само пространство пар — `research/logoped_sochetaemost_prostranstvo_2026-08-27.txt`.

1. **Страница `/otzyvy` под ключом** — не сделана. Нужна, чтобы отзывы читались с телефона и
   Клодом, без панели хостинга. Должна: склеивать двойное «нет» · переводить время в местное ·
   прятать тестовые записи · разворачивать `sound`/`material`/`profile` словами.
2. **Google-форма** — ждёт от автора `GOOGLE_FORM_URL` и `GOOGLE_FORM_FIELD`.
3. **LogoKonspekt лежит** — публичное HF-пространство отдаёт Runtime error, годовалая ссылка из
   видеообзора Дарьи ведёт на трейсбек. Обёртка публична и правится: заменить на страницу
   «проект вырос в logozvuk.com».
4. **Слоговая дорожка** — переделка не начата (см. выше).
5. **Word-выгрузка**: слоговая дорожка уходит на два листа. Слово автора: «пока сойдёт».
6. **Яндекс.Вебмастер и Search Console** — последнее из ШАГа 6 канона.
7. **Стечение в конце (АРТ)** на прописи — не будет никогда: линия и есть тянущийся звук,
   согласный обязан стоять в начале пути.
8. **Показать живому логопеду** — единственное, чего нельзя сделать за столом.


## Сделано 08-25 — день выхода наружу

**Сайт собран и опубликован.** Шесть страниц вышли из черновиков, `sitemap.xml` наполнился,
черновики честно отдают 404.

**Подвал заполнен.** Почта `igor@fanarlabs.com` (временно, до почты на своём домене), соцсеть —
ТГ-группа `t.me/logo_konspekt`. **Адрес пропущен сознательно**: канон просит его ради траста
у поисковиков, но это технический фактор, а не требование закона; выдуманный адрес в подвале
русскоязычного сервиса был бы ложью. Код к пустой строке готов.

**Канал обратной связи (`web/otzyv.py`, `/api/otzyv`).** Два входа: тихая ссылка
**«На листе что-то не так?»** (всегда, под кнопкой печати) и один вопрос после ВТОРОГО
полученного листа — **«Вы дадите этот лист ребёнку?»** (формулировка автора). «Нет» отправляется
сразу, до причин; к каждому отзыву пишется контекст (звук · материал · отмеченные звуки · сколько
листов взято). Персональных данных не собираем.

**Яндекс.Метрика, счётчик 111927540.** Стоит и на страницах, и в виджете — в виджете под
оговоркой `window.top===window.self`, иначе один заход считался бы дважды. Вебвизор намеренно
выключен. ⚠ Первая установка была сломана дважды (лишняя скобка в коде счётчика и `document.write`
со строкой, содержащей закрывающий тег скрипта) — обе поймал автор глазами.

**Знак проекта — `[р]`**, звук в квадратных скобках: логопедическая нотация. Прежняя заглушка
читалась как значок вай-фая. Знак с именем стоит в левом верхнем углу и ведёт на главную.

**Собранная раскладка виджета.** Диагноз был не в отступах: сетка растягивалась на всю ширину
окна, и на 1440 закладки уезжали влево, панель вправо, между листом и панелью зияла дыра.
Рабочая область ограничена 1180 px и центрирована, панель собрана в столбец.

**Кнопка «Скачать» запоминает формат** (PDF · Word · печать), выбранный виден на самой кнопке.
**Плашка «текст можно править»** переехала в правый верхний угол и ждёт прикосновения, а не
таймера. С листа звуковой дорожки снята строка про тип слога; экран отказа больше не повторяет
причину и называет ступень словами («звук между гласными»), а не глифом «АСЯ».

**PDF снова на одной странице.** Причина была не в конвейере (Gotenberg печатает как `print`
и берёт поля из `@page`; печатная область ровно 273 мм — замерено ступенчатой пробой), а в
модели высоты заданий: она считает строки по средней ширине символа и обещала 77,5 мм при
бюджете 80,5, а живой перенос рвёт строку раньше. Бюджету дан запас `FIT_SLACK_MM = 8`.
Проверено настоящими PDF: 11 звуков × 3 позиции лабиринта и все шесть материалов — везде одна
страница. Цена: игра «четвёртый лишний» снимается теперь на всех позициях, даже там, где влезала.

**Word: рисунки стали картинками.** Word не отображает inline SVG, а весь наш рисунок — SVG;
теперь перед выгрузкой каждый SVG рисуется в canvas и уходит PNG (размер берётся из атрибутов
в миллиметрах, а не с экрана — иначе он зависел бы от зума). Лабиринт пересобирается в таблицу
3×3 по координатам клеток. ⚠ Стрелки лабиринта в Word не переносятся сознательно.

**В логах видно роботов.** Знакомые получают короткую метку (`[yandexbot]`, `[telegrambot]`),
незнакомые — своё имя целиком; человек остаётся без пометки. Отличаем по `Accept-Language`.

**⚠ Телеграм не строит превью ссылки — причина не найдена.** Проверено и исключено: разметка
Open Graph (прочитан сырой `<head>`), картинка 1200×630 отдаётся, скорость, DNS и IPv6,
редиректы, HEAD, сжатие, сертификат (независимое сканирование: A−, цепочка без замечаний),
российский хостинг (у `vc.ru` превью есть), кеш по домену (`www` — новый домен, тоже пусто).
**В логах при этом ни одного запроса от бота Телеграма — он не приходит вовсе.** Осталась
непроверяемая версия: домену три дня. Проверить через неделю. На рассылку не влияет: пост идёт
с тремя настоящими картинками, они крупнее превью.

## Что работает

**Движок** — `logoped_slovar/`, чистый stdlib, ноль зависимостей.
Тесты: `test_content` (2244) · `test_phonetics` (148) · `test_maze` (251) ·
`test_capabilities` (167) · `test_story` (1353) · `test_rasskaz` (1054) — зелёные, 5217 проверок
(пересчитано 08-24). Чеклист канона `check_sheets.py` — 0 нарушений.

**Веб** — `web/`, порт 8784, живёт на Railway (картинки на хосте проверены живьём 08-18).
Рабочих материалов шесть; три ненаписанных (4-й лишний · зашумлённые · обводка) стоят блоком
«будет позже» на шаге «что делаем», а не в линейке рабочих вкладок.
⚠ Сервер держит код в памяти: **после правки движка перезапускать**, иначе автор не увидит
изменений и решит, что их нет.

**Тупик «Рассказы» закрыт 08-23** (нашёл автор). Выбранный рассказ и тема переживали смену звука
и профиля, лист упирался в отказ, из которого не было выхода, и лечило только обновление страницы.
Проверено вживую по всем трём путям (звук · профиль · тема); та же болезнь у фона, позиции лабиринта
и игры листа проверена и не найдена. Разбор — `DECISIONS.md`, 2026-08-23.

### Спрайты фона: замер закрыл развилку (08-22)

**Из 35 спрайтов сцены 11 не ложатся на лист НИ РАЗУ.** Заказывать под них картинки — платить
за то, чего логопед не увидит. Замер воспроизводим: `python3 logoped_slovar/measure_scenes.py`,
отчёт — `research/sprity_ukladka_2026-08-22.md`.

**Число кружков ни при чём — проверено.** На 30, 25 и 20 кружках погибают ровно те же 11: высота
холста считается из числа рядов, поэтому, убавляя ряды, мы убавляем и холст. **Старая развилка
«срезать кружки с 30 до 25» ради фона закрыта — она не помогает.**

**Что ограничивает на самом деле — обрыв на ширине 10-12 мм.** Спрайт не шире 10 мм может быть
высоким (до 34 мм), спрайт шириной 12 мм и больше упирается в 13 мм по высоте, и дальше ширина
почти ничего не меняет. Отсюда **две законные формы**: узкая вертикаль (≤10 мм шириной) или
низкая горизонталь (≤12 мм высотой). Это объясняет весь список без исключений: камыш 9×24 живёт,
полотенце 12×20 умирает — разница в трёх миллиметрах ширины.

**Предложение по всем одиннадцати посчитано и проверено укладкой** (ёлка 15×28 → 10×28, ванна
40×18 → 24×12 и так далее, таблица в отчёте). ⚠ Решает автор, а не замер: ёлка 10×28 — ёлка
вытянутая, солнце 18×12 — уже овал. Годится ли такая форма, видно только на пробном отпечатке.

### Сцена научилась класть растр (08-22) — конвейер спрайтов замкнут

**Механизм готов и проверен на трёх картинках.** `Canvas.image()` кладёт предмет картинкой из
банка `pictures/fony/`, отдавая её **адресом** `/fony/<имя>.png`, а не base64: сцена «трава»
кладёт 53 предмета, встраивание раздуло бы один лист на мегабайт. **Вектор не удалён, а остался
запасом** — у каждого из 35 спрайтов первая строка теперь «есть картинка → положить и выйти»,
нет файла → рисуется как раньше. Замер: без банка сцена даёт 63 вектор-пути и 0 растра, с банком —
19 и 11. Тест на это есть (`test_scene_sprite_raster_with_vector_fallback`).

**Доводчик линии — `imgbw.thicken()`.** Наращивает тёмное, пока медиана штриха не дойдёт до
печатных 0.95 мм. ⚠ Величина подбирается ЗАМЕРОМ: формула «наращивание на r даёт +2r» промахнулась
втрое (1.45 · 2.55 · 1.10 при цели 0.95), потому что наращивание СЛИВАЕТ соседние линии — у ванны
с решёткой крана ошибка вышла восьмикратной. Подбором вышло 1.00 · 1.00 · 0.96.

**Боксы приведены к замеру:** ёлка 15×28 → 10×18, солнце 18×18 → 13×13, ванна 40×18 → 20×12.
Высота берётся из собственной пропорции предмета, а не с потолка укладки: растянутая ёлка
перестаёт быть ёлкой. После правки **мёртвых спрайтов стало 8 вместо 11**.

**Посмотрено глазами:** слоговая дорожка на [ж] со сценой «лес» — одиннадцать растровых ёлок вперемешку
с векторными кустами, грибами и облаками; смешение читается одним фоном. Банк едет в Docker-образ,
сервер отдаёт его маршрутом `/fony/` с кэшем на сутки.

**Что осталось по спрайтам:** заказать оставшиеся 32 (у восьми мёртвых сперва поправить боксы),
прогнать через доводчик и посмотреть глазами. ⚠ Печатную проверку доводки может сделать только
автор: на экране читаются оба варианта, разница между экраном и дешёвым принтером и есть предмет
спора.

### Проба спрайтов: толщина линии промптом не берётся (08-22)

Заказаны три пробы в новых формах (`tools/gen_kartinki.py` + `logoped_slovar/scenes_prompts.json`,
≈3 цента). **Раннер лежит В GIT** — в отличие от скрипта, которым гнали 979 картинок корпуса и
которого нет ни в одном коммите.

**Форма верна, боксы надо править:** у предмета своя пропорция, и «узкая вертикаль до 34 мм» —
это потолок укладки, а не форма ёлки. Ель шириной 10 мм естественно выходит 18 мм высотой →
бокс `10×18`, а не `10×28`. Ванна → `20×12`. Солнце `13×13` село точно.

**⚠ Главное: толщину линии выпросить нельзя.** Сцена печатает 0.95 мм; генератор дал 0.32-0.48 мм,
а после втрое более настойчивой формулировки («широким чёрным маркером, ошибись в сторону слишком
толстого») — 0.58-0.67 мм. До нужного не дошёл ни разу. Значит толщину доводим КОДОМ: наращивание
линии в `imgbw` с проверкой замером, пока медиана штриха не дойдёт до 0.95 мм на бумаге.
**Пока доводчик не написан, словарь целиком заказывать нельзя** — 35 картинок пропадут с бледного
фона, и платить придётся дважды.

**Поправлена своя же ошибка:** сначала предложил солнце и покрышку овальными (18×12, 15×12) — это
против локального закона 11, узнаваемость решает форма предмета. Замер показал, что круг 13×13
влезает: обоим достаточно стать мельче, не меняя формы.

### Иллюстрации страниц сайта сняты (08-22)

**Восемь снимков настоящих листов** лежат в `web/img/` и отдаются с сайта по `/img/<имя>.png`:
лист на занятие · домашний лист · лист с профилем ребёнка (иллюстрация рва — видно, как уходят
слова с непоставленными звуками) · слоговая дорожка · звуковая дорожка · словосочетания ·
лабиринт · рассказ. Канон требует минимум три иллюстрации на страницу и называет картинку
фундаментом статьи, а не украшением.

Инструмент — `tools/snimki_listov.py`: просит материал у живого сервера через `/api/*` (тем же
путём, каким его получает логопед), вшивает картинки героя как data:-URI и снимает headless-Chrome
в размер A4 с двойной плотностью, потом уменьшает до 1200 px. ⚠ Вшивание не украшение: без него
Chrome с `file://` не пускает запрос на `http://localhost`, и герой выходит битым квадратом
(поймано глазами на первом снимке 08-22).

**Значок вкладки** — `/favicon.ico` и `/favicon.svg`, заглушка без буквы и имени: знак проекта
за автором. До этого 404 сыпался на каждой странице.

### Скелет сайта построен (08-22)

**Сайт живёт на нашем же сервере, отдельным слоем от виджета.** Виджет переехал с корня в
**каталог `/app/`** — канон: каталог переливает больше веса, чем поддомен. Корень принадлежит сайту.
Встраивается со своего же origin: `<iframe src="/app/?sound=r&material=list&embed=1">`, поэтому ни
X-Frame-Options, ни CSP чинить не пришлось — сервер их не ставит вовсе.

**Где что лежит.** `web/sitepages.py` — реестр 11 страниц (slug · h1 · title · description ·
родитель · параметры виджета · черновик) плюс шаблон, разметка и подвал. `web/pages/*.html` — тело
каждой страницы, **его пишет автор**: канон запрещает писать текст страниц ИИ прямо и дважды, поэтому
в файлах лежат брифы, а не проза. `web/site.css` — стиль страниц, к виджету отношения не имеет.
⚠ Модуль назван `sitepages`, а не `site`: `site` — имя модуля стандартной библиотеки, Python грузит
его до нашего кода, и `import site` молча отдаёт чужой модуль (поймано запуском 08-22).

**Карта страниц** (по канону ШАГов 3-4-7, звук — КАТЕГОРИЯ, не тег; тегов на старте нет):
главная `/` · `/avtomatizaciya-zvuka-r/` (денежная, 4 099 точной/мес) · `/avtomatizaciya-zvuka-r/doma/` ·
`/profil-rebenka/` (сквозная, здесь доказательство софтовости) · `/docs/` + четыре дока ·
`/o-proekte/` · `/kontakty/`. Вторая звуковая категория (Л·Ш·С) НЕ добавляется, пока нет методики —
движок между звуками различий не даёт, а канон запрещает дублирование подстановкой буквы.

**Все 11 страниц — ЧЕРНОВИКИ.** Черновик снаружи неотличим от несуществующей страницы: 404, нет в
карте сайта, нет в подвале. Это не гигиена, а бюджет обхода: полупустая страница, единожды попавшая
в индекс, тянет вниз весь сайт. Пока корень отдаёт виджет, как и до сайта, — прод не ломается
оттого, что текст ещё не написан.

**Что сервер теперь умеет:** страницы по slug со слешем на конце (второй вид адреса — один 301, не
цепочка) · человеческая 404 с `noindex` · `/robots.txt` (закрыты `/api/` и `/geroi/`) ·
`/sitemap.xml` (только живые страницы) · canonical, Open Graph и JSON-LD (WebSite + WebPage +
Organization + BreadcrumbList) из шаблона, а не руками на каждой странице · 301 с любого чужого хоста
на канонический — этим же снимается `logozvuk.ru` и `www`. Адрес сайта берётся из **`SITE_URL`**
в окружении: домен куплен позже сервера, вшивать его в код — переписывать при переезде.

**Адресат листа в адресе:** `?audience=doma` открывает домашний лист (движок и экран умели давно,
не хватало карты адреса). Мусорное значение молча падает в умолчание.

⚠ **Условие запуска, не заглушка:** словарь `PROJECT` в `sitepages.py` пуст в трёх полях — почта на
своём домене, полный адрес с индексом, ссылка на соцсеть. Канон требует все пять элементов подвала.
Пока пусты — страницы остаются черновиками.

Проверено 08-22: sitemap отдаёт 11 адресов, `/docs/list` даёт ровно один 301 на `/docs/list/`,
404 отвечает 404 и несёт `noindex`, `title` не равен `h1`, `h1` на странице один, canonical и
`og:url` совпадают, крошки строятся. Тесты движка зелёные.

### Виджет умеет вставать в страницу сайта (08-21)

**Адрес страницы говорит, с чем открыть виджет:** `/?sound=l&material=labirint&embed=1`.
Звуки латиницей (`r · rj · l · lj · s · sj · z · zj · sh · zh · shch` — мягкость хвостом «j»,
апостроф в адресе нельзя), материалы словами (`list · slogovaya-dorozhka · zvukovaya-dorozhka ·
slovosochetaniya · labirint · rasskaz`). Все 11 звуков и все 6 материалов покрыты, проверено
сверкой карты с `/api/config`. **Неизвестное значение молча падает в умолчание, а не в ошибку** —
по адресу придут робот и случайная ссылка (закон 12). Витринных материалов в карте нет намеренно:
адрес не имеет права открывать то, чего движок не соберёт.

**`embed=1`** прячет шапку с вкладками и снимает ограничение ширины — канон Fullwidth: виджет от
края до края в начале страницы. Класс вешается при разборе скрипта, а не в `boot()`, иначе шапка
успевает мигнуть.

**Кэш разведён по смыслу ответа** (был `no-store` на всём): движок — `no-store`, код и разметка —
`no-cache` с ETag (браузер спрашивает, но получает 304 без тела), картинки банка — сутки.
Прежний комментарий у героя обещал кэш, которого при `no-store` не было: при каждом открытии
страницы заново ехали 95 КБ кода плюс цветной герой 73-229 КБ. Скорость получения пользы канон
считает прямым фактором ранжирования.

Проверено в браузере: [Л]+лабиринт, [Ш]+слоговая дорожка в embed, [Щ]+словосочетания, мусорные
параметры → лист на [р]; ошибок в консоли нет; 304 по ETag приходит.

### Экран перестроен вокруг листа (08-22/23, решения автора)

**Три колонки: закладки звуков · лист · панель.** Верхней полосы нет вовсе.

**ЗВУК — вертикальные закладки** у левого края, все 11 всегда на виду, переключение одним нажатием.
Отдельный экран выбора звука снят. Причина словами автора: «непонятно, если я на листе Л, а решил
перейти на Р — как мне это сделать? интуитивно ничто не подсказывает, что нужно нажать на „звук Л“».

**ЛИСТ ЦЕЛИКОМ, БЕЗ ПРОКРУТКИ.** Масштаб считается по двум пределам сразу — высота окна и ширина
колонки, — а не по одной ширине. ⚠ Ключевое, что нужно помнить при любой правке раскладки: **лист
упирается в ВЫСОТУ окна, а не в ширину.** При окне 800 px он физически не может быть шире ~560 px.
Поэтому «дать листу больше места» невозможно; можно только не отдавать весь остаток панели — отсюда
средняя колонка тянется и центрирует лист, а панель ограничена сверху (`clamp(320px, 27vw, 440px)`).
Отступы страницы сверху и снизу сняты: каждый их пиксель отнимался у листа.

**ПАНЕЛЬ без коробок.** Настройка = заголовок + контрол. Сняты: строка «16 слов из 159», заголовок
«Этот лист», подпись под слогами (тип ушёл в ховер кнопки), хвост подсказки чипов, подписи под
переключателями, все крошки, экраны «что делаем» и «какой звук». Смысловые настройки идут сверху,
ряд «как печатать» (Домой · Цветной лист) отделён чертой внизу — по слову автора «смысловые
настройки по одной части, а форматирование отдельно». «Цветной герой» переименован в «Цветной лист».

⚠ **Три ловушки раскладки, найденные ЗАМЕРОМ, а не глазом** — если сетка снова поедет, искать здесь:
1. `hidden` слабее любого правила с классом: настройка с `display:flex` перестаёт прятаться.
   Держится правилом `[hidden]{display:none!important}`.
2. Элемент сетки не может стать уже своего содержимого без `min-width: 0`. Полоса вкладок в панели
   (650 px) распирала всю раскладку на 275 px за край экрана, и лист уезжал вправо обрезанным.
   На глаз это читается как «лист почему-то обрезан»; находится только по `scrollWidth`.
3. Ширину листа считает ТОЛЬКО `fitFrame`. Правило `width:100%!important` в узкой раскладке
   перебивало расчёт и ломало лист. Один хозяин у размера.

**Узкая раскладка (≤820 px):** закладки ложатся горизонтальным рядом, под ними ЛИСТ, и только потом
настройки. Прежнее правило ставило панель первой, и до листа было полтора экрана прокрутки — прямо
против решения 08-21. Проверено на 390×844: вылета нет, лист целиком и виден сразу.

### Как устроен виджет (08-21, решение автора)

**Лист приходит СОБРАННЫМ.** Экрана выбора звука перед листом нет и промежуточного «что делаем» нет:
логопед видит бумагу, а не следующий вопрос. Умолчание — звук [р] и лист автоматизации (единственный
материал, проходящий ступени внутри себя). Звук, материал и ось — переключатели рядом с листом.
Обоснование не из головы: разобран 31 чужой генератор, мастеров в поле два из двадцати девяти, и оба
оплачены кассой или подпиской; отрасль отвечает на «параметров много» одним длинным экраном.
Ближайшие рифмы — calendar-12.com, 1Password, incompetech, страница результата worksheetgenius
(`research/site/analogi_generatorov_2026-08-19.md`).

- **Профиль ребёнка переживает смену материала.** Прежде `pickMaterial` делал `S.profile.clear()` —
  ров продукта незаметно обнулялся на каждом переключении вкладки. Теперь профиль — часть задачи.
- **Кубик у пяти материалов** (лист · слоговая · звуковая · словосочетания · рассказ). Движок
  принимал seed давно, кнопка стояла только у листа. Лабиринт не включён намеренно: на узких пулах
  seed даёт перестановку тех же слов, а кнопка, которая ничего не меняет, — та же ложь, что
  погашенная без причины.
- **Гласная звуковой дорожки перебирается**: seed 0-4 даёт ра · ро · ру · ры · рэ. Раньше печаталось
  всегда «РА» — экран не слал seed, хотя сервер его принимал.
- **Герой звука цветной и рисованный**, стоит в шапке блока [2] листа и у старта обеих дорожек.
  Отдаётся файлом по `/geroi/<bw|colour>/<имя>.png`, а не встраивается: ч/б весит 2-3 КБ, цветной
  73-229 КБ. Умолчание ч/б, кнопка «Цветной герой» — у листа и дорожек. У лабиринта своё умолчание
  (цвет), потому что там цвет нужен для узнавания картинки.
- ⚠ Высота героя учтена в расчёте листа. Не учесть — лист уедет на вторую страницу: ровно так 08-12
  четырнадцать конфигураций из двадцати пяти печатались в два листа.

### Где выбор из двух — назван остаётся один (08-18)

Правило автора (закон 13): пара сегментов стоит на экране трижды — два названия плюс
заголовок-вопрос. Переделаны три пары: **куда лист** (молчит «на занятие», кнопка «Домой») ·
**рассказ** (молчит пересказ, кнопка «Сочини сам») · **ступень звуковой дорожки** (молчит
«звук + слог», кнопка «Только звук»). Кнопка залипает вдавленной, подсказка под ней в обоих
состояниях говорит, какой лист выйдет. Проверено в браузере: все три переключают лист,
крошка «слог» уходит на изолированной ступени, ошибок в консоли нет.
⚠ Дефолт листа сменился: было «домой», стало «на занятие».

Групп из трёх и более правило не касается (игры · тексты · темы · фоны) — там заголовок
несёт вопрос, на который отвечает набор.

### Экран знает, что материал умеет — до нажатия (08-10 · 08-11)

Один дом правды — `logoped_slovar/capabilities.py`, человеческие формулировки — `web/human.py`:

- **слоги**: у листа пять, у слоговой дорожки три, у звуковой один (только прямой).
  Недоступные кнопки погашены с причиной; **вкладки материала, который текущий слог не
  делает, вообще нет** (решение автора: «раз её нет, не надо и показывать»);
- на кнопке слога стоит **весь ряд** (ЩА · ЩЁ ЩУ ЩИ), а не один пример: гласная меняется,
  звук держится (08-11);
- **бедные слоги помечены**: «Слов в картотеке мало: 8 из 12 канонных — лист выйдет бедным»;
- **игры блока [5]**: живые считаются по словам ЭТОГО листа (`games_offer`), мёртвая кнопка
  погашена с числом, горит та игра, что реально на бумаге; молчаливой подмены выбора нет;
- **позиции лабиринта**: «в конце слова» погашена у [З] [Зь] [Ж] (оглушение) и у [Сь] [Щ]
  (не набирается девяти картинок).

### Фон — контекст героя, и он строится вокруг дорожки (08-10)

| герой (звук) | миры |
|---|---|
| мотор [р · рь] | дорога · город |
| самолёт [л · ль] | небо · аэродром |
| насос [с · сь] | двор · гараж |
| комарик [з · зь] | лес · пруд |
| змея [ш] | трава · камни |
| жук [ж] | трава · лес |
| щётка [щ] | ванная · прихожая |

«Без фона» доступно всегда. Сцен 13; «море» осталось в банке, но не закреплено ни за кем —
ждёт водного героя. Дорожка отдаёт сцене карту занятых мест, сцена **просит место у холста**
(`scenes.Canvas`): свободно — печатается, занято — нет. Горизонтали режутся вокруг кружков
сами. Отсюда сцена заполняет промежутки между рядами и **ложится по-разному на разных
листах**. **Форма пути идёт от сцены**: улица — прямые с поворотом углом, тропинка — вьётся,
комната — спокойная волна.

⚠ Про сцены помнить: не должно быть предмета, который просится быть названным · узнаётся
силуэт, а не текстура · густое лучше редкого · декор конкурирует с задачей за внимание
(Fisher, Godwin & Seltman 2014: в украшенном классе дети вне задачи 38,6 % против 28,4 %).

### Типографика (08-11)

Все материалы на одном гротеске (PT Sans → Helvetica → Arial). Georgia убрана из
слоговой дорожки — она была единственной антиквой. Основание: две российские нормы для
дошкольников (СанПиН 2.4.7.960-00 п. 4.2.1 · ОСТ 29.127-96 п. 5.1.2) плюс практика
(BDA Style Guide 2023). Отчёт — `research/logoped_vizual_kanon_2026-08-10.md`.

**Словари и таблицы:**

| что | сколько | что даёт |
|---|---|---|
| существительные | 1598 на 11 звуков | ядро листа, дорожки, лабиринт |
| глаголы | 356 (322 несов.) | предложения |
| прилагательные | 253 (103 с антонимом) | словосочетания |
| родительный падеж | 590 ед. ч. / 595 мн. ч. | игра «Посчитай 1-5» |
| уменьшительные | 306 форм + 269 в стоп-листе | игра «Назови ласково» |
| ударение мн. ч. | 49 | чистоговорка |
| сочетаемость прил.+сущ. | 33 класса, 213 слов | словосочетания |

Ц и Ч не входят намеренно: аффрикату нельзя тянуть, и порядок слогов у них обратный.
Недоборы у з · з' · ж · щ — свойство языка (у звонких нет позиции конца слова).

**Семь материалов** — построчно в `INSTRUMENTY.md`. Звуковая дорожка — **только прямой
слог** и ступень «только звук». В словосочетаниях с 08-11 стоят **ударения** (та же политика,
что на листе слов). Герой на дорожке склоняется («Проведи щётку», не «щётка»), путь называется
от сцены (по дороге · по улице · по полосе · по тропинке · по дорожке).

---

## Ревизия 08-23 — что найдено и что уже закрыто

**Закрыто в тот же день (тесты зелёные, глазами проверено):**
- ✅ `extra_questions` вошли в проверку чистоты (`rasskaz.check_text`). Было: блок «Ещё
  поговорите» не проверялся вовсе, и **17 текстов из 31** ставили ребёнка на звук, который
  подвал этого же листа обещает не давать. Ложь стояла на главном обещании продукта. Все 17
  вопросов переписаны и прогнаны валидатором.
- ✅ «Кого?» о неодушевлённом — 3 вопроса заменены на выбор («Поросёнок нюхает сено или осину?»).
  Правило в шапке `rasskaz.py` надо разделить: «фонетически чистые» ≠ «применимые».
- ✅ Шесть текстов с доказанной бессмыслицей переписаны (zs-01 «зелёная как зерно» и «лепит
  газетой» · zs-02 · s-01 круг без события · z-03 нулевое событие · ss-04 опись вместо рассказа ·
  shch-02 ящерка со щепкой), три правки в одну строку (zh-02 «жарил на дороге» · rs-02 финал
  против текста · sh-02 «шишка-ёлка»). Валидатор: 0 падений из 31.
- ✅ Вкладка «Лист» → **«Занятие»** (единственное изданное слово для составной единицы —
  Коноваленко); кнопка «Домой» → **«Лист на дом»**. Решение автора 08-23.

**Закрыто 08-24 (продолжение того же разбора):**
- ✅ мусорные кнопки слогов — оказались НЕ мусором: «аСьмь» структурно та же «альт» у Спивак.
  Настоящая беда была другая и тоже закрыта: на звуковой дорожке ряд из пяти кнопок стоял там, где
  движок принимает ОДИН тип (`PROPISI_TYPES = ("direct",)`) — карточка убрана целиком;
- ✅ причина погашенного слога вышла из ховера на экран и стала втрое короче (тексты писались для
  ховера, где объём ничего не стоит);
- ✅ «Как это устроено» расклеена: `renderMethod()` стоял внутри `if (!METHOD)` и справка замерзала
  на первой открытой вкладке; у «Рассказов» секции не было вовсе;
- ✅ стечения на слоговой дорожке открыты профилем (решение автора): рамки берутся из тех же слов
  и тем же отбором, что у листа;
- ✅ пятый класс фонем — смычные П Б Т Д;
- ✅ спрайты фона: 32 картинки заказаны, доведены, банк укомплектован (35 из 35);
- ✅ счётность в игре «Один — много».

**Осталось (не закрыто):**
- 🔴 **z-02 «Замок» печатается без ударения** — омограф, в картотеке помечен `ambiguous`.
  Нужно поле ручного ударения у текстов либо переименование рассказа.
- 🔴 **Словарный фильтр (familiarity/age_min) к рассказам не применяется вообще** — тексты пишутся
  рукой под фонетический валидатор, а словарный порог обходится: на бумагу попадает лексика,
  которую тот же движок на листе слов отверг бы.
- 🔴 **Слоги: машинный мусор на кнопках** — живые кнопки «аСьмь», «аРьг», «аЗьй», «аЩнь»
  у четырёх звуков; объяснение к погашенному слогу спрятано в tooltip (`app.js:269`), тогда как
  у лабиринта и игр та же причина выведена строкой на экран.
- 🔴 **Прочерк у стечения после гласной врёт причиной** (см. ниже).
- 🔴 **`capabilities.py` расходится со Спивак**: у неё пятёрка полна только для Ш, Л, Р; у Ль/Рь
  четыре, у Ж три, у Щ стечений нет вовсе. У нас все пять живы у всех звуков.
- 🔴 **Наш отчёт 08-01 опровергнут первоисточником**: у Р Спивак печатает обратное стечение в обе
  стороны («атр-отр-утр» И «арт-орт-урт»), а отчёт это запрещал правилом. Проверить, не режем ли
  напечатанный материал.
- 🔴 **«Как это устроено» — замороженный ящик**: `renderMethod()` заперт в `if (!METHOD)`
  (`app.js:1236`), справка рисуется один раз; у «Рассказов» секции нет вовсе и fallback подсовывает
  справку листа.

**Замер стечения после гласной по всей картотеке** (к вопросу о прочерке):
Р 8 · Л 8 · С 7 · Ш 3 · **З 2** (гнёздышко, пазл — оба перед звонким) · **Ж, Зь, Рь, Щ — 0**.
У [З] и [Ж] лист говорит «слов не набирается», а причина языковая: то же оглушение, что и
у обратного слога.

## 🔴 Прежний список ревизии

1. **Блок «Ещё поговорите» не проверяется на чистоту вовсе.** Валидатор `rasskaz.check_text`
   берёт текст, заголовок и `questions` (строка 192-193), а `extra_questions` в список не входит —
   хотя `render_rasskaz` их печатает. Замер (пустой профиль, воспроизводится
   `C.banned_phonemes(snd, frozenset())`): **17 текстов из 31** ставят ребёнка на звук, который
   этот же лист в подвале обещает не давать. На листе [Р] спрашивают «Что ты строил?» — «строил»
   несёт [Л], снятый как смешиваемый с [Р]. Это ложь на главном обещании продукта (ров), а не
   опечатка. Проверено дважды: агентом и вручную.
2. **«Кого?» о неодушевлённом — 6 текстов.** Аграмматизм вынужден фонетикой: «что» несёт [ч] и
   на листах [с] [с'] [з] [з'] [ж] [щ] запрещено (шапка `rasskaz.py:52-54`), а «кого» объявлено
   «чистым всегда» — фонетически верно, грамматически нет. «Кого нюхает поросёнок?» → сено.
   Ребёнок обязан повторить ошибку: ему велено отвечать полным предложением.
3. **Смысловой брак в 10 текстах (⛔) и спорный в 13 (🔶)**, чистых 5. Разбор с готовыми заменами
   строк — в отчёте ревизии. Общий диагноз: **валидатор ловит звуки и не ловит смысл**, и там, где
   правильного слова на листе нет, рука подставляет неправильное молча.
4. **Слоги: кнопки печатают машинный мусор** — живые кнопки «аСьмь» (из «восьмёрка»), «аРьг»,
   «аЗьй», «аЩнь» у четырёх звуков из одиннадцати. Плюс объяснение к погашенному слогу спрятано
   в tooltip (`app.js:269`), тогда как у лабиринта и игр та же причина выведена строкой на экран.
5. **Прочерк у стечения после гласной врёт причиной.** У [З] и [Ж] лист говорит «слов не
   набирается», а причина языковая — то же оглушение, что и у обратного слога. Замер по всей
   картотеке: Р 8 · Л 8 · С 7 · Ш 3 · **З 2** (гнёздышко, пазл — оба перед звонким) · Ж, Зь, Рь,
   Щ — 0.

---

## Открытое

- ✅ **закрыто 08-12: ложный обратный слог у мягких.** В `occurrence_kind` передавали ряд
  КОЛОНОК, и им же проверялось «есть ли гласная после звука». У мягкой цели ряд короткий
  (а·ы·и·э), поэтому «берёза» — где после [р'] стоит [о] — считалась обратным слогом и
  печаталась в колонке «Ирь:». Было 6 слов из 16 на листе [Рь] и 5 из 9 на [Сь].
  Теперь структурный вопрос задаётся полным набором гласных. Цена: у [Сь] обратный слог
  осел до 4 слов и гаснет по канону — пополнять нечем, двойной разбор нашёл только
  «ось · пропись · подпись · надпись · морось», и это не слова для детского листа;
- **стечение перед гласной у щелевых** печатает «кса · рша · нща» — целевой звук вторым,
  канон требует первым. Корень найден: `occurrence_kind` исключает `cluster_role == "first"`
  из `cluster_onset`. Правка одной строки, но трогает отбор слов везде;
- **10 сочетаний ниже канона ступени** (после правки 08-11): щ/cluster_coda — 1 слово из 7,
  з'/cluster_coda · р'/cluster_coda · с'/cluster_coda — по 3, с/cluster_onset — 4.
  Пополнением не лечится: язык не даёт столько детских слов со стечениями;
- 🔶 **у Спивак обратный ряд девять гласных** (а · о · у · ы · и · е · ё · ю · я), у нас
  четыре-пять. Не ошибка (у нас работает правило «слоговой блок ≤ ¼ словесного»), но разница
  в объёме ступени — осознать отдельно;
- **улитка-спираль** четвёртой формой звуковой дорожки — у Поповой названа простейшей;
- **три листа из 19 без чистоговорки** (Ж · Зь · Сь прямые) — концевой рифмы нет в языке;
- **«Скажи наоборот» не строится** — после фильтра 0–12 пар на звук, у ш · ж · щ · з' ноль;
- **41 слово размечено разными категориями в разных словарях** («золото» — «материалы» в [з]
  и «предметы» в [л]);
- ✅ **закрыто 08-17: рассказы.** Запрос Ольги («генерация рассказов») закрыт материалом
  `rasskaz.py` — готовый текст для пересказа; рядом остался `story.py` («сочини сам»),
  и он **выверен по канону 08-12**: 3-4 слова в наборе (Глухов 2014, с. 72), три набора на
  выбор (Усова), план из трёх вопросов с их функциями (Пеньевская, цит. Алексеева/Яшина
  с. 291), обязательный образец начала от взрослого (Глухов: без картинок образец нужен
  постоянно). Прежние «6-9 слов» стояли на ложной атрибуции Ефименковой — снято.
  ⚠ Осознать: по канону это **верхняя ступень** (Филичева 1989: … → рассказы → разговорная
  речь), а предыдущие — пересказ готового текста с вопросами — у нас невозможны. И возраст
  у Глухова 6-7 лет, не 5.
  Остальные четыре стоят не на глаголах, а на двух блокерах: формат «вставь слово по картинке»
  ждёт банк картинок, а «рассказ-задача», «рассказ с вопросами» и «готовый рассказ» требуют
  СВЯЗНОГО ТЕКСТА — движок же проверяет чистоту только слов своей картотеки, произвольный текст
  разобрать не умеет (`phonetics.analyze` отказывает без размеченного ударения). Собрать рассказ
  из каркасов блока [6] можно, но выйдет «У мамы рама. Тут рак и роза.» — список под видом
  рассказа, что запрещено законом 2;
- ✅ **закрыто 08-18: предметные картинки.** Своих 982 — весь пул слов, какие лабиринт может
  поставить в клетку (замер по пулу, а не по выборке). Цвет и ч/б у каждого слова, ч/б снимается
  с цветного. Проверка: ноль слов без картинки, в том числе на урезанных профилях. Этим же банком
  закрывается блокер 4-го лишнего, зашумлённых и обводки;
- **дифференциация** (68 % объёма пособия Спивак, у нас ноль);
- **лабиринт**: картинки теперь свои и выкладывать их можно; развилка `ambiguous` отложена;
- ✅ **хостинг поднят 08-12** — `logoped-generator-production.up.railway.app`, Railway, деплой
  из GitHub по push. HuggingFace отпал: Docker-Space больше нельзя создать без PRO $9/мес.
  Railway выбран не ценой, а сетью: отдаёт из своей (`server: railway-hikari`), Cloudflare в
  пути нет — а тот с июня 2025 отдаёт российским провайдерам только первые 16 КБ ресурса, и
  все 150 логопедов в России. Проверено живьём: экран открывается, лист собирается на
  хостинге, HEAD отвечает 200 (был 501 — по нему стучатся превью-боты Telegram).
  **Не проверено:** открывается ли с домашнего или мобильного интернета в РФ — мерили с
  хостинговых узлов, а логопеды сидят не там. Перед постом в канал нужен один живой логопед
  без VPN. Домен привязывать только DNS-only (серое облако), иначе сами уедем под троттлинг.

---

## Чем держится материал — и где дыры

| часть | опора |
|---|---|
| 7 блоков листа | ✅ замер трёх альбомов + карточка практика |
| фильтр по звукам | ✅ канон Коноваленко, проверка падает при нарушении |
| образы звуков (мотор, комарик, щётка) | ✅ Фомичёва / Спивак, с провенансом |
| гимнастика и подсказки «как делать» | ✅ Фомичёва 1989 |
| «вставь слог» · «исправь взрослого» | ✅ Жихарева, с. 36 и с. 9 |
| шкала длины предложений | ✅ Спивак 2007 |
| словосочетания | ✅ Спивак — пункт во всех 10 разделах, замер 45 пар |
| механика звуковой дорожки | ✅ Борисова 2008 · три задачи голосу — Попова 2022 |
| **только прямой слог у звуковой дорожки** | ✅ 10 образцов практика + формула приёма (08-10) |
| **слоговые ряды со стечением** | ✅ Спивак, дословно (08-11) |
| **минимум 7 слов на стечении** | ✅ Спивак, строка «Стечение согласных: штанга, шкаф…» |
| **гласная Ы в обратном слоге** | ✅ Спивак «аш - ош - уш - ыш…» + расчёт по картотеке |
| **формы слов в играх** | ✅ таблицы двойного разбора; правило выключено |
| **гротеск вместо антиквы** | ✅ СанПиН 2.4.7.960-00 · ОСТ 29.127-96 · BDA 2023 |
| **сдержанность фона** | ✅ Fisher, Godwin & Seltman 2014 |
| **рисунки персонажей** | ⚠ наши, образ канонный — рисунок нет |
| **фоны и их наполнение** | ⚠ наши; «менять фон» — строка запроса Ольги |
| **форма линии дорожки** | 🔶 рисунок наш, источник требует только непрерывности |
| 7 клеток отметок в шапке | 🔶 практика логопунктов, в пособиях не найдены |

---

## Осталось доспросить источником

1. **Один ли образ у твёрдой и мягкой пары** (мотор / моторчик) — или у мягких свой?
   Сейчас мягкая берёт рисунок твёрдой, и это наше решение;
2. **Живые формулировки задания слоговой дорожки** у практиков;
3. **Кегль**: норма 20 pt против канона методики — конфликт, решение за автором (см. выше).

---

## Развилки, ждущие автора

0д. **🔴 СЧЁТНОСТЬ РАЗМЕЧЕНА ТОЛЬКО ЧАСТИЧНО — 14 слов из ~1900.**
   Автор поймал 08-24 на слове «лёд»: игра «Один — много» стояла на признаке
   «форма мн. ч. существует», а держится на другом — «предмет можно посчитать».
   У вещественных «много» даёт СОРТА (соки, каши, колбасы), у омонимов другое
   слово («душ» → «души» от «душа»), у омографов другое ударение («замок»).
   Размечены руками те 12 слов, что реально доходили до игры (14 карточек — два
   слова лежат в двух словарях). **Полная разметка всех существительных с
   `plural` — отдельная работа двойным разбором**, как делали падежи 08-10.
   До неё игра может снова вынести вещественное, если состав листа сменится.



0в. **✅ ПРАВКА ЛИСТА НА МЕСТЕ — СДЕЛАНА 08-24.** Автор выбрал форму В (свободный ввод),
   не А и не Б. Лист редактируем ВСЕГДА, без кнопки режима: печать идёт из этого же документа,
   а кубик рядом уже означает «собрать заново», то есть отмена существует. Правка ломает
   обещание подвала — поэтому первая же правка дописывает туда оговорку «Лист правлен вручную:
   изменённое генератор не проверял», и она уходит в печать. Плюс кнопка «ударе́ние» (U+0301
   с клавиатуры не набрать, а движок им печатает — без неё правка руками умела бы меньше, чем
   сам генератор). Пересборка листа сбрасывает правки и говорит об этом строкой.
   PDF отдельной кнопки не получил: он внутри окна печати и даёт тот же файл.
   НЕ сделано: перемещение графики (форма В второй половины) и Word.

   Прежний разбор развилки:

0г. **ПРАВКА ЛИСТА НА МЕСТЕ (Diffit) — разбор.** Мысль автора из ведра 08-23: «готовый лист
   редактируется прямо на месте — правишь текст/задания в самом листе, не пересобирая».

   **Почва проверена живьём 08-23, это факты, а не оценка:**
   - каждое слово блока [4] — уже отдельный узел `<div class="w">`, их 21 на листе;
   - печать идёт из документа рамки (`app.js` → `f.contentWindow.print()`), значит правка
     в DOM **уходит в печать даром** — проба: «рак» → «ПРОБА», в печатном документе есть.

   **Но три вещи ломаются, и первая — ровно та болезнь, что чинили утром:**
   1. любая настройка или кубик зовут `writeFrame`, `srcdoc` переписывается, **правки
      исчезают молча**. Земля уходит из-под правки без предупреждения;
   2. подвал листа обещает «слова подобраны так, чтобы в них не было звуков, которые у
      ребёнка сейчас не получаются». Логопед вписал слово рукой — обещание стало ложью.
      **Это тот же класс, что 17/31 в блоке «Ещё поговорите»**, и бьёт по рву — единственному,
      чем мы отличаемся от бумажного альбома;
   3. числа рва в панели перестают описывать бумагу.

   **Diffit копировать один в один нельзя:** у него материал — текст для чтения, обещания о
   фонетическом составе нет, и правка безопасна. У нас обещание ЕСТЬ и оно и есть продукт.

   **Три формы, по возрастанию цены:**
   - **А. Выключить лишнее.** Клик по блоку или строке — гаснет и не печатается. Чистоту не
     нарушает вовсе (ничего не добавляется). Дёшево. Вероятно закрывает бо́льшую часть нужды:
     убрать логопеду надо чаще, чем вписать.
   - **Б. Замена из разрешённого.** Клик по слову — список ДРУГИХ чистых слов той же колонки
     (движок их уже отобрал). Ввода с клавиатуры нет, обещание держится механически. Заодно
     закрывает «перекатить одно слово» вместо всего листа.
   - **В. Свободный ввод + живая проверка.** Логопед печатает что хочет, движок считает чистоту
     на лету и подсвечивает. Обещание держится не запретом, а видимостью. Дороже всего; и
     подвал обязан сменить формулировку на «правки логопеда генератор не проверял».

   **Голос Клода:** А сейчас, Б следующим, В — только если тест на людях покажет, что без
   свободного ввода не обойтись. И В — это ШАГ 7 канона («виджет до продакшена»), не ШАГ 2.
   В любом случае: если беремся, правка обязана переживать перерисовку или честно предупреждать.

   Строка в `garden-meta/INBOX.md` оставлена — её разберёт полный бокер.



0. ✅ **ЗВУКОВАЯ ДОРОЖКА — развилка закрыта 08-24.** Автор выбрал «три ступени по Поповой»;
   затем открыт её первоисточник (файл слайдов), и оба наших чтения сняты — «улитка = первая
   строка» и «три строки = три её варианта». Строки: плавная → зигзаг → пунктирные горки, тир
   упаковки 🔶. Герой один, лист заполнен, умолчание «только звук», гласная кнопками.

0б. **Витрина трёх ненаписанных материалов** — нужна ли логопеду до постройки. С 08-22 не видна
   нигде: экран «что делаем», где она стояла, снят. Ставить её в ряд рабочих вкладок нельзя —
   решение автора 08-18: «неработающая кнопка читается как поломка».


Перенесено из `TZ.md` 08-18; сам орган закрыт 08-23, остаток живого — в `INSTRUMENTY.md`:
- **С6** — как называем два материала, чтобы не спорить с языком клиента;
- **С1/С3** — догонять ли образец по числу кружков на слоговой дорожке и делать ли фон;
- **Ф** — вводить ли «фронтир» (докуда дошли по этому звуку) вместо нынешнего «всегда полный
  лист». Разбор — `DECISIONS.md`, 2026-08-04.


1. **`ambiguous`** — утюжок · творожок · ножовка: ребёнок назовёт картинку базовым словом
   («утюг»), где целевого звука нет. Честная пометка урежет словари вдвое. Отложено 08-06.
2. ~~Интерфейс как замысел~~ — **закрыто 08-22/23.** Экран перестроен вокруг листа по
   реальным образцам (incompetech · timeanddate · mazegenerator) и по словам автора; разбор и
   список правок — DECISIONS 08-22 и 08-23.
3. **Герой [Р] зовётся «мотор», а нарисована машина.** У всех остальных имя и рисунок
   совпадают. Предложение Клода: на дорожке звать героя «машина», в блоке [2] листа оставить
   канонную «песенку мотора». Ждёт слова автора.
